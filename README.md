# Staker Airdrop Scripts

Scripts:
- `distribute.ts`: Read in `airdrop.csv`, write `completed_airdrops.csv`
- `drop.ts`: Calculate addresses to airdrop to. Write `oven.csv` and `voters.csv`.

Running:
```
$ npm i -g ts-node
$ ts-node <script>
```

